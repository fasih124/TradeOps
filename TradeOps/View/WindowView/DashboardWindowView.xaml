<Window
    x:Class="TradeOps.View.WindowView.DashboardWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="clr-namespace:TradeOps.Helper"
    xmlns:local="clr-namespace:TradeOps.View.WindowView"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DashboardWindowView"
    Width="800"
    Height="450"
    FontFamily="{materialDesign:MaterialDesignFont}"
    Style="{StaticResource MaterialDesignWindow}"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="7.4*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="2"
            Grid.ColumnSpan="1"
            Margin="20,25,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            FontSize="24"
            FontWeight="Bold"
            Text="Trade Track" />
        <Grid
            Grid.RowSpan="2"
            Grid.Column="0"
            Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />

            </Grid.RowDefinitions>

            <Button
                Grid.Row="0"
                Width="40"
                Height="40"
                Margin="0,20,0,0"
                Background="Transparent"
                BorderThickness="0"
                Focusable="False"
                Foreground="Black"
                IsHitTestVisible="False"
                Style="{StaticResource MaterialDesignIconButton}"
                ToolTip="Menu">
                <materialDesign:PackIcon
                    Width="24"
                    Height="24"
                    Kind="Menu" />
            </Button>

            <ListBox
                x:Name="NavigationListBox"
                Grid.Row="2"
                Margin="0,30,0,10"
                VerticalAlignment="Top"
                SelectedIndex="0"
                SelectionChanged="NavigationListBox_SelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style BasedOn="{StaticResource MaterialDesign3.NavigationRailListBoxItem}" TargetType="ListBoxItem">
                        <Setter Property="materialDesign:NavigationRailAssist.IsTextVisible" Value="True" />
                        <Setter Property="materialDesign:NavigationRailAssist.SelectedIcon" Value="{Binding SelectedIcon}" />
                        <Setter Property="materialDesign:NavigationRailAssist.UnselectedIcon" Value="{Binding UnselectedIcon}" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.Resources>
                    <DataTemplate DataType="{x:Type domain:SampleItem}">
                        <TextBlock Text="{Binding Title}" />
                    </DataTemplate>
                </ListBox.Resources>
            </ListBox>
        </Grid>

        <ContentControl
            Name="MainContent"
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="1" />

        <Button
            Grid.Row="2"
            Height="50"
            Margin="0,0,0,20"
            Background="White"
            BorderBrush="Transparent"
            Click="Logout"
            Foreground="Black"
            Style="{StaticResource MaterialDesignFlatLightButton}">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <materialDesign:PackIcon
                    Width="24"
                    Height="24"
                    Margin="0,0,8,0"
                    Kind="Logout" />
                <TextBlock VerticalAlignment="Center" Text="Exit" />
            </StackPanel>
        </Button>

    </Grid>
</Window>
