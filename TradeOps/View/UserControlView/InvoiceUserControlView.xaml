<UserControl
    x:Class="TradeOps.View.UserControlView.InvoiceUserControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TradeOps.View.UserControlView"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    FontFamily="{materialDesign:MaterialDesignFont}"
    mc:Ignorable="d">
    <Grid Margin="10" Background="{DynamicResource MaterialDesignPaper}">
        <DataGrid
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding Invoices}"
            SelectedItem="{Binding SelectedInvoice}"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ID}" Header="Invoice ID" />
                <DataGridTextColumn Binding="{Binding OrderID}" Header="Order ID" />
                <DataGridTextColumn Binding="{Binding TotalPrice}" Header="Total Price" />
                <DataGridTextColumn Binding="{Binding TotalProfit}" Header="Profit" />
                <DataGridTextColumn Binding="{Binding Discount}" Header="Discount" />
                <DataGridTextColumn Binding="{Binding IsPaid}" Header="Paid" />
                <DataGridTextColumn Binding="{Binding Date}" Header="Date" />
                <DataGridTextColumn Binding="{Binding Date}" Header="Action" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
