<UserControl x:Class="TradeOps.View.UserControlView.InvoiceUserControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TradeOps.View.UserControlView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Invoices}" 
                  AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedInvoice}"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice ID" Binding="{Binding ID}" />
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}" />
                <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice}" />
                <DataGridTextColumn Header="Profit" Binding="{Binding TotalProfit}" />
                <DataGridTextColumn Header="Discount" Binding="{Binding Discount}" />
                <DataGridTextColumn Header="Paid" Binding="{Binding IsPaid}" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" />
                <DataGridTextColumn Header="Action" Binding="{Binding Date}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
